name: holdable_circular_button
description: "A customizable Flutter widget for a circular button with a progress indicator."
version: 0.0.4
homepage: "https://github.com/arifxpartbd/holdable_circular_button"

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/screenshot.png/